<%- model_class = SupplementSubCategory -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:name) %></th>
      <th><%= model_class.human_attribute_name(:supplement_category_id) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @supplement_sub_categories.each do |supplement_sub_category| %>
      <tr>
        <td><%= link_to supplement_sub_category.id, supplement_sub_category_path(supplement_sub_category) %></td>
        <td><%= supplement_sub_category.name %></td>
        <td><%= supplement_sub_category.supplement_category.name %></td>
        <td><%=l supplement_sub_category.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_supplement_sub_category_path(supplement_sub_category), :class => 'btn btn-primary btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      supplement_sub_category_path(supplement_sub_category),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-danger btn-xs' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_supplement_sub_category_path,
            :class => 'btn btn-primary' %>
