<%- model_class = DayEntry -%>

<div class="page-header">
  <h2><%= Date.today.strftime("%B") %> Entries</h2>
</div>


<%= month_calendar do |date| %>
  <%= date.day %> <br />
  <% if DayEntry.today(date) %>
    <%= link_to "✅ Day", day_entry_path(DayEntry.today(date)) %>
  <% else %>
    <%= link_to "Add day", new_day_entry_date_path(date) %>
  <% end %>
  <br />
  <% if NightEntry.today(date) %>
    <%= link_to "✅ Night", night_entry_path(NightEntry.today(date)) %>
  <% else %>
    <%= link_to "Add night", new_night_entry_date_path(date) %>
  <% end %>
<% end %>
